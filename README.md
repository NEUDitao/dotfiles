# dotfiles
A loose compilation of my dotfiles, just so I can clone them across multiple devices. Nothing fancy, I rarely customize things.

## Currently has:
  - .bash_profile from work Mac
  - .emacs file (so uggglllyyy)
